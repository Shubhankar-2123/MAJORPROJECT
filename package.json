{
  "name": "major-project-data",
  "version": "1.0.0",
  "description": "This repository hosts a Flask-based application and supporting scripts for static (image) and dynamic (video) sign recognition, plus utilities for dataset preprocessing and model training.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:tailwind": "tailwindcss -i tailwind_input.css -o flask_app/static/css/tailwind.min.css --minify",
    "watch:tailwind": "tailwindcss -i tailwind_input.css -o flask_app/static/css/tailwind.min.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
